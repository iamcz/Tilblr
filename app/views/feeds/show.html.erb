<section class="group" id="blog">
</section>

<script>
$(function () {
  // var blogId = <%= @blog.id %>;

  // Tilblr.initialize(blogId, currentUserId);

  var currentUserId = <%= logged_in? ? current_user.id : "undefined" %>;
  var feedId = <%= @blog.id %>;
  var feed = new Tilblr.Models.Feed({id: feedId});
  var feedView = Tilblr.Views.FeedShow;
  //var feedView = new Tilblr.Views.FeedView({model: feed});
  
  Tilblr.initialize({
    model: feed,
    view: feedView,
    userId: currentUserId
  });
});
</script>
