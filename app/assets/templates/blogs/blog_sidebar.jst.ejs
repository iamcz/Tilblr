<header class="info-header group">
  <h1><%= blog.escape("title") %></h1>

  <% if (!blog.ownedByCurrentUser()) { %>
    <section class="follow-container"></section>
  <% }; %>
</header>

<h2><%= blog.owner().escape("username") %></h2>

